@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <EmptyContent1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private EmptyContent1Block PreviewBlock => EmptyContent1BlockMapper.FromNode(Node);
}
