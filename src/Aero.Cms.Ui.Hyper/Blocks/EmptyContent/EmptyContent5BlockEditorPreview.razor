@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <EmptyContent5BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private EmptyContent5Block PreviewBlock => EmptyContent5BlockMapper.FromNode(Node);
}
