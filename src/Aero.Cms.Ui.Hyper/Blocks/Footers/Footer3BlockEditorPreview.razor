@using Aero.Cms.Abstractions.Blocks.Neo
@using Aero.Cms.Ui.Hyper.Blocks.Footers

<div class="pe-neo-preview">
    <Footer3BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Footer3Block PreviewBlock => Footer3BlockMapper.FromNode(Node);
}
