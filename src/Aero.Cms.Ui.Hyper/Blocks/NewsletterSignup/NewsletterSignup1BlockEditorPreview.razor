@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <NewsletterSignup1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private NewsletterSignup1Block PreviewBlock => NewsletterSignup1BlockMapper.FromNode(Node);
}
