@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <NewsletterSignup2BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private NewsletterSignup2Block PreviewBlock => NewsletterSignup2BlockMapper.FromNode(Node);
}
