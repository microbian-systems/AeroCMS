@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <FeatureGrids2BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private FeatureGrids2Block PreviewBlock => FeatureGrids2BlockMapper.FromNode(Node);
}
