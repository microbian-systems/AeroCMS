@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <FeatureGrids1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private FeatureGrids1Block PreviewBlock => FeatureGrids1BlockMapper.FromNode(Node);
}
