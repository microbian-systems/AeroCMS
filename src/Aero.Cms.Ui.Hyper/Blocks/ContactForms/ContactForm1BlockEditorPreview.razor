@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ContactForm1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ContactForm1Block PreviewBlock => ContactForm1BlockMapper.FromNode(Node);
}
