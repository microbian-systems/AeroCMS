@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ContactForm2BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ContactForm2Block PreviewBlock => ContactForm2BlockMapper.FromNode(Node);
}
