@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ContactForm5BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ContactForm5Block PreviewBlock => ContactForm5BlockMapper.FromNode(Node);
}
