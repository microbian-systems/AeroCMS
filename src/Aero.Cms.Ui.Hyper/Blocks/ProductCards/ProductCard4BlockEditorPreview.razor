@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ProductCard4BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ProductCard4Block PreviewBlock => ProductCard4BlockMapper.FromNode(Node);
}
