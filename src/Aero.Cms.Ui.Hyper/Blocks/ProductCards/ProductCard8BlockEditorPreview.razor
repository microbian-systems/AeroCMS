@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ProductCard8BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ProductCard8Block PreviewBlock => ProductCard8BlockMapper.FromNode(Node);
}
