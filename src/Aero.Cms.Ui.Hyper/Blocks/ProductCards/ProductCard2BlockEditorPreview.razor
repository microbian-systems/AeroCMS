@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ProductCard2BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ProductCard2Block PreviewBlock => ProductCard2BlockMapper.FromNode(Node);
}
