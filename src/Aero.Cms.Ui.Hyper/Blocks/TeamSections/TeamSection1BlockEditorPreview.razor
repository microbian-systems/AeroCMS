@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <TeamSection1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private TeamSection1Block PreviewBlock => TeamSection1BlockMapper.FromNode(Node);
}
