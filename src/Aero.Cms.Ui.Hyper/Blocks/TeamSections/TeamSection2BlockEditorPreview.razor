@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <TeamSection2BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private TeamSection2Block PreviewBlock => TeamSection2BlockMapper.FromNode(Node);
}
