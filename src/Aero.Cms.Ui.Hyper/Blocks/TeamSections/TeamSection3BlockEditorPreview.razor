@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <TeamSection3BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private TeamSection3Block PreviewBlock => TeamSection3BlockMapper.FromNode(Node);
}
