@using Aero.Cms.Abstractions.Blocks.Neo
@using Aero.Cms.Ui.Hyper.Blocks.Buttons

<div class="pe-neo-preview">
    <Button7BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Button7Block PreviewBlock => Button7BlockMapper.FromNode(Node);
}
