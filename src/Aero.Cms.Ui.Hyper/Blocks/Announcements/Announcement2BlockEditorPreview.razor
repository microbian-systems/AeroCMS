@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <Announcement2BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Announcement2Block PreviewBlock => Announcement2BlockMapper.FromNode(Node);
}
