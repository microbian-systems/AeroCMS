@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <Announcement3BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Announcement3Block PreviewBlock => Announcement3BlockMapper.FromNode(Node);
}
