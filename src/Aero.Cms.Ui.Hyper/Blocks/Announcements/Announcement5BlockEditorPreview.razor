@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <Announcement5BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Announcement5Block PreviewBlock => Announcement5BlockMapper.FromNode(Node);
}
