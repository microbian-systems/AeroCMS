@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <Announcement1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Announcement1Block PreviewBlock => Announcement1BlockMapper.FromNode(Node);
}
