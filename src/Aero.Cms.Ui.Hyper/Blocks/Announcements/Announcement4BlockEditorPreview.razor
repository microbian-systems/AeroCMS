@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <Announcement4BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private Announcement4Block PreviewBlock => Announcement4BlockMapper.FromNode(Node);
}
