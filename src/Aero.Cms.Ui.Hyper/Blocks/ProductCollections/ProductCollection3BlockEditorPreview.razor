@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ProductCollection3BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ProductCollection3Block PreviewBlock => ProductCollection3BlockMapper.FromNode(Node);
}
