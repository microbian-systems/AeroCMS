@using Aero.Cms.Abstractions.Blocks.Neo

<div class="pe-neo-preview">
    <ProductCollection1BlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private ProductCollection1Block PreviewBlock => ProductCollection1BlockMapper.FromNode(Node);
}
