@page "/manager/taxonomy/tags"
@attribute [Authorize]

@code {
    [Inject] private NavigationManager Navigation { get; set; } = default!;

    protected override void OnInitialized()
        => Navigation.NavigateTo("/manager/taxonomy?kind=tags", replace: true);
}
