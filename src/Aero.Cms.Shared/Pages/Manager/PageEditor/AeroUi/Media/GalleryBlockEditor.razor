@using Aero.Cms.Abstractions.Blocks.Neo
@inject IStringLocalizer<Aero.Cms.Shared.Localization.ManagerResource> L

<div class="pe-property-panel">
    <div class="pe-property-group">
        <label class="pe-property-label">@L["Images ({0} total)", Block.Images.Count]</label>
    </div>
    @for (int i = 0; i < Block.Images.Count; i++)
    {
        var index = i;
        <div class="pe-property-group">
            <label class="pe-property-label">@L["Image {0}", index + 1]</label>
            <RadzenTextArea @bind-Value="Block.Images[index]" Class="w-full" Rows="2" />
        </div>
    }
    <div class="pe-property-group">
        <label class="pe-property-label">@L["Columns"]</label>
        <RadzenNumeric @bind-Value="Block.Columns" Min="1" Max="6" Class="w-full" />
    </div>
</div>

@code {
    [Parameter, EditorRequired]
    public GalleryBlock Block { get; set; } = default!;
}
