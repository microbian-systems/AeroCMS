@using Aero.Cms.Abstractions.Blocks
@using Aero.Cms.Abstractions.Blocks
@using Aero.Cms.Abstractions.Blocks.Common
@using Aero.Cms.Abstractions.Blocks.Editor
@using Aero.Cms.Abstractions.Blocks.Neo
@using Aero.Cms.Shared.Blocks.Rendering

<AeroAuthRenderer Block="PreviewBlock" />

@code {
    [Parameter] public NeoPageNode? Node { get; set; }
    [Parameter] public EditorBlock OwnerBlock { get; set; } = default!;

    private AeroAuthBlock PreviewBlock => new()
    {
        Title = FirstNonEmpty(OwnerBlock.MainText, OwnerBlock.Title),
        Description = OwnerBlock.Description,
        SubmitButtonText = OwnerBlock.CtaText,
        AlternativeLinkText = OwnerBlock.AlternativeLinkText,
        AlternativeLinkUrl = OwnerBlock.AlternativeLinkUrl,
        BackgroundImageUrl = OwnerBlock.BackgroundImage
    };

    private static string FirstNonEmpty(params string?[] values) =>
        values.FirstOrDefault(v => !string.IsNullOrWhiteSpace(v)) ?? string.Empty;
}
