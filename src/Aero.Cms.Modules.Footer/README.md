# Aero CMS Footer Module

Provides site-scoped public footer management for AeroCMS.

The module stores footer changes as Marten event streams, projects the current
footer documents inline, exposes manager minimal APIs, and renders the default
published footer through an MVC ViewComponent.
