<p class="neo-primitive-text-preview">@Text</p>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private string Text =>
        Node.Properties.TryGetValue("text", out var value)
            ? value.GetString() ?? string.Empty
            : string.Empty;
}
