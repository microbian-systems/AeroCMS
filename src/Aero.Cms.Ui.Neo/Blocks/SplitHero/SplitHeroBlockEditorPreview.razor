<div class="pe-neo-preview pe-neo-hero-preview">
    <SplitHeroBlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private SplitHeroBlock PreviewBlock => SplitHeroBlockMapper.FromNode(Node);
}
