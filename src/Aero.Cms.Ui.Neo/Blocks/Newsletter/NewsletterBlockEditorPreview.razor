<div class="pe-neo-preview pe-neo-newsletter-preview">
    <NewsletterBlockRenderer Block="PreviewBlock" />
</div>

@code {
    [Parameter, EditorRequired]
    public NeoPageNode Node { get; set; } = default!;

    private NewsletterBlock PreviewBlock => NewsletterBlockMapper.FromNode(Node);
}
