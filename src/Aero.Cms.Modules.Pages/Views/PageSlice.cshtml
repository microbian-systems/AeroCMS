@using Aero.Cms.Shared.Blocks.Rendering
@model Aero.Cms.Modules.Pages.PageDocument

@foreach (var region in Model.LayoutRegions.OrderBy(r => r.Order))
{
    <component type="typeof(LayoutRegionRenderer)" render-mode="Static" param-Region="region" />
}
