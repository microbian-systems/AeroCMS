@page
@model Aero.Cms.Modules.Pages.Areas.Cms.Pages.DynamicPageModel
@section cssPartial {
    <style type="text/tailwindcss" 
           src="/_content/Aero.Cms.Modules.Pages/css/pages.css"></style>
    <style type="text/css" 
           src="/_content/Aero.Cms.Modules.Pages/css/aero-pages.css"></style>
}
@section headPartial {
    <link rel="canonical" href="@Model.CanonicalUrl" />
    @foreach (var link in Model.AlternateLinks)
    {
        <link rel="alternate" hreflang="@link.Hreflang" href="@link.Href" />
    }
}
@{
    ViewBag.Title = Model.SeoTitle ?? Model.Title ?? "Page";
    ViewBag.ShowHeaderNavigation = Model.ShowHeaderNavigation;
    ViewBag.HideFooter = Model.HideFooter;
    ViewBag.ShowChatAgent = Model.ShowChatAgent;
}

@* <h1>Hydro Component Should show Below</h1>
<page-counter /> *@

@if (Model.Title is null)
{
    <div class="min-h-screen flex items-center justify-center bg-gray-50">
        <div class="text-center">
            <h1 class="text-4xl font-bold text-gray-900 mb-4">Page Not Found</h1>
            <p class="text-gray-600">The requested page could not be found.</p>
        </div>
    </div>
}
else
{
    <article class="min-h-screen bg-white">
        <div class="max-w-7xl mx-auto px-4 py-16 sm:px-6 lg:px-8">
            @if (Model.RootNode is not null)
            {
                <component type="typeof(Aero.Cms.Shared.Blocks.Rendering.NeoNodeRenderer)"
                           render-mode="Static"
                           param-Node="@Model.RootNode"
                           param-NestingDepth="0"
                           param-MaxDepth="8" />
            }
            else if (Model.LayoutRegions.Count > 0)
            {
                foreach (var region in Model.LayoutRegions.OrderBy(r => r.Order))
                {
                    <component type="typeof(Aero.Cms.Shared.Blocks.Rendering.LayoutRegionRenderer)" render-mode="Static" param-Region="@region" />
                }
            }
            else
            {
                @* Empty State *@
                <div class="flex flex-col items-center justify-center py-16 bg-gray-50 rounded-2xl border-2 border-dashed border-gray-300">
                    <svg class="h-16 w-16 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
                    </svg>
                    <h3 class="mt-4 text-lg font-medium text-gray-900">No Content Yet</h3>
                    <p class="mt-2 text-center text-gray-500">This page doesn't have any content blocks configured.</p>
                </div>
            }
        </div>
    </article>
}

@* Chat Agent Widget *@
@if (ViewBag.ShowChatAgent == true)
{
    <component type="typeof(Aero.Cms.Shared.Components.ChatAgentButton)" render-mode="Static" />
}
